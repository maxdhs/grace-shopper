import { useState } from "react";
import { useNavigate } from "react-router-dom";

const Kids = ({}) => {
  const history = useNavigate();
  return (
    <>
      <h4>All Kid's clothing</h4>
    </>
  );
};
export default Kids;
