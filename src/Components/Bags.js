import { useState } from "react";
import { useNavigate } from "react-router-dom";

const Bags = ({}) => {
  const history = useNavigate();
  return (
    <>
      <h4>All Bags and Wallets</h4>
    </>
  );
};
export default Bags;
